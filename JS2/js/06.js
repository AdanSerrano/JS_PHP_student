//objeto math
// escribimos windows en el navegador y aparece todos los objetos
let resultado;

resultado = Math.PI;
resultado = Math.round(2.5);// redondea despues del 2.5 
resultado = Math.ceil(2.5);// redondea al siguiente numero si tiene un desimal
resultado = Math.floor(2.5);// redondea a la unidad
resultado = Math.sqrt(144);  // imprimi  la raiz cuadrada 
resultado = Math.abs(-132); // compierte a positivo
resultado = Math.min(3, 5, 1, 8, 10); // numero menor de un listado
resultado = Math.max(3, 5, 1, 8, 10); // numero maximo de un listado
resultado = Math.random();
resultado = Math.floor(Math.random() * 40);



console.log( resultado );